# AI 自媒体运营工作台 PRD

## 1. 产品定位
本产品是一个本地优先、可部署到个人域名的 AI 自媒体运营工作台，用于统一完成账号管理、热点扫描、内容生成、搜图、预览审校和多平台发布。第一阶段定位为“半自动工作台”，默认所有内容先进入预览和编辑，再决定是否发布。

## 2. 产品目标
- 将现有 CLI 工具升级为前后端分离的可视化系统。
- 统一管理多个平台账号及其凭证状态。
- 基于热点快速生成标题、正文、提示词和图片检索词。
- 提供草稿保存、预览、发布记录和失败回溯能力。
- 为未来 SKILL 调用和 MCP 接入预留扩展口。

## 3. 非目标
- 第一阶段不做复杂多角色权限系统。
- 第一阶段不实现完整 MCP 调用链路。
- 第一阶段不追求全自动无人值守发布。

## 4. 目标用户
- 个人创作者
- 多账号运营者
- 小型内容工作室

## 5. 核心业务流程
1. 登录系统
2. 在配置中心填写 Cookie、AI API、模型和默认参数
3. 在账号中心新增或切换平台账号，并验证 Cookie 有效性
4. 在热点中心扫描头条、百度、Google 等热点
5. 选择热点并触发搜图、标题生成、文章生成或 SKILL 调用
6. 在编辑器中修改标题、正文、配图和提示词
7. 在预览页按平台效果审查内容
8. 保存草稿或发布，并记录执行结果

## 6. 页面与模块

### 6.1 登录页
- 单管理员账号登录
- 支持后续扩展为域名访问时的基础认证入口

### 6.2 首页 / 工作台
- 展示账号总览、待处理热点、最近草稿、最近发布记录

### 6.3 系统配置中心
- 配置 AI API Key、模型名、接口地址
- 配置平台 Cookie、浏览器路径、默认生成参数
- 支持保存后校验和状态反馈

### 6.4 平台账号中心
- 使用卡片形式展示账号
- 字段包括平台、账号名、头像、登录状态、粉丝数、昨日收入、最后验证时间、失败原因
- 昨日收入支持隐藏/显示切换
- 支持同平台多账号新增、编辑、切换、设为默认
- 支持 Cookie 失效接管流程：当百家号、微信公众号等账号凭证同时失效时，系统可启动受控浏览器会话，进入登录页待人工接管，登录完成后回收最新 Cookie 并更新账号状态

### 6.4.1 Cookie 失效接管流程
- 触发条件：百家号、微信公众号 Cookie 校验失败，且达到“需要人工重新登录”的状态
- 系统动作：自动启动无头或受控浏览器，打开对应平台登录页或扫码页
- 现场反馈：抓取当前浏览器页面截图，回传到工作台界面；后续可扩展为通过飞书发送提醒与截图
- 人工动作：管理员在浏览器中完成扫码或登录
- 回收动作：系统检测登录完成后提取最新 Cookie，写回 SQLite，并重新执行一次有效性校验
- 状态要求：全过程记录开始时间、平台、处理状态、最近截图、失败原因

### 6.5 热点扫描中心
- 聚合头条、百度、Google 等热点源
- 支持关键词、来源、时间筛选
- 每条热点支持收藏、入库、查看原始链接
- 每条热点支持一键搜图和生成图片关键词

### 6.6 AI 创作中心
- 基于热点生成候选标题、文章大纲、正文、封面提示词、配图提示词
- 支持指定平台和指定账号生成
- 支持多种风格，如资讯解读、争议型、评论型、故事化

### 6.7 SKILL 中心
- 展示本地已安装或已登记的 SKILL
- 每个 SKILL 展示名称、说明、适用平台、输入参数、最近调用结果
- 支持选择平台和账号执行 SKILL
- 示例：生成容易引起讨论的头条文章
- 第一阶段先实现列表、查看和调用入口，执行编排留后续扩展

### 6.8 编辑与预览中心
- 支持草稿编辑、图片替换、版本保存
- 支持按平台渲染预览
- 支持发布前确认、敏感词校验、字数提示

### 6.9 发布记录中心
- 展示平台、账号、内容标题、发布时间、状态、失败原因
- 支持失败重试和结果追踪

## 7. 核心数据对象
- 用户：用于基础登录认证
- 系统配置：AI、浏览器、默认生成参数
- 平台账号：平台类型、账号标识、凭证、状态、运营数据
- 热点：来源、标题、摘要、热度、链接、抓取时间
- 图片候选：关键词、来源、预览图、选用状态
- 内容草稿：标题、正文、图片、提示词、平台、账号、状态
- 发布记录：平台、账号、内容、结果、错误信息
- SKILL：名称、说明、适用范围、参数定义
- SKILL 调用记录：输入、输出、状态、耗时

## 8. 技术选型

### 8.1 统一语言
- 前端语言：TypeScript
- 后端语言：TypeScript
- 选择原因：统一数据模型和接口定义，最大化复用现有 Node.js 逻辑，降低改版成本

### 8.2 前端
- 框架：React
- 构建工具：Vite
- 路由：React Router
- 数据请求：TanStack Query
- 状态管理：Zustand
- UI 方案：Tailwind CSS + 可复用组件体系

说明：`.cline/frontend-design.md` 更适合作为前端视觉与交互实现规范，适用于 React、Vue 和原生页面，不绑定单一框架。结合本项目，建议统一为 `React + TypeScript`。

### 8.3 后端
- 框架：NestJS
- 运行时：Node.js
- ORM：Prisma
- 数据库：一期默认 SQLite，后续部署可切 PostgreSQL

选择 NestJS 的原因：
- 适合认证、配置、账号、热点、SKILL、发布等多模块系统
- 天然支持 TypeScript 和分层结构
- 易于从现有 Node.js 脚本逐步迁移逻辑

### 8.4 认证
- 一期使用单管理员账号登录
- 后端签发会话或 JWT，并通过 HttpOnly Cookie 保存登录状态
- 后续再扩展多用户、角色与权限

### 8.5 任务与扩展
- 生成、扫描、发布任务先走应用内任务流
- 后续可引入队列系统
- MCP 先预留统一调用接口，不在一期实现

## 9. 分阶段 Todo

### P0：产品与架构确认
- 完成 PRD、页面清单、导航结构
- 确定前端 React + TypeScript、后端 NestJS + TypeScript
- 确定数据库模型和 API 草案
- 确定认证方案、配置方案、SKILL 预留接口

### P1：基础可用版本
- 搭建前端项目和后端项目骨架
- 实现登录页和基础认证
- 实现系统配置中心
- 实现平台账号卡片管理和 Cookie 校验
- 实现热点扫描列表
- 实现热点搜图入口
- 实现基于热点的标题/文章/提示词生成
- 实现草稿保存和预览页

### P2：工作流打通
- 接入真实平台账号状态读取
- 展示粉丝数和昨日收入，并支持隐藏
- 实现同平台多账号切换
- 实现发布记录与失败重试
- 实现 SKILL 列表和按平台、账号调用入口
- 实现 Cookie 失效接管流程：检测百家号、微信公众号 Cookie 同时失效后，启动浏览器、截图回传到页面，并在人工登录后抓取新 Cookie

### P3：增强与扩展
- 支持域名部署场景
- 增加多用户和权限
- 增加更多热点源和更多平台
- 实现 MCP 接口
- 增加运营数据分析与报表
- 接入飞书通知：将 Cookie 失效告警、浏览器登录截图、人工接管提醒发送到指定会话

## 10. 当前建议
先不要直接重写所有旧逻辑，而是优先搭建 Web 产品骨架，再逐步把现有 `src/` 中的能力拆分迁移到 NestJS 服务层。这样可以保留现有可工作的生成与发布逻辑，同时控制改版风险。
